info:
  name: ask rag
  type: http

http:
  method: POST
  url: http://127.0.0.1:8000/ask
  body:
    type: json
    data: |-
      {
        "query": "What items are in invoice 0012820?"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​